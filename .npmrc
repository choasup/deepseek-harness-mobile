# 这台机器到 registry 的连接会随机 ECONNRESET，导致可选原生依赖
# （如 @rollup/rollup-darwin-arm64）静默缺失，而 pnpm 仍报成功。
# 加大重试与超时，让安装自己扛过去。
fetch-retries=8
fetch-retry-mintimeout=10000
fetch-retry-maxtimeout=90000
fetch-timeout=180000
